{
  "name": "tsops-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo nothing to build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "tsops": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "vitest": "^3.2.4"
  }
}
